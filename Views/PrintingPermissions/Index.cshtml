@model IEnumerable<MicrobiologyLab.Models.PrintingPermission>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.page_number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.coloredOrNot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.user)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.page_number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.coloredOrNot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.printing_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.printing_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.printing_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
